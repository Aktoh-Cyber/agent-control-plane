# 🚀 How to Run AgentDB CLI

## Quick Commands (from ANY directory):

### Option 1: Change to the correct directory first

```bash
cd /workspaces/agent-control-plane/agent-control-plane
node dist/agentdb/cli/agentdb-cli.js --help
```

### Option 2: Use the launcher script (from anywhere)

```bash
/workspaces/agent-control-plane/agent-control-plane/run-agentdb.sh --help
```

### Option 3: Create a global alias (add to ~/.bashrc)

```bash
alias agentdb-new='node /workspaces/agent-control-plane/agent-control-plane/dist/agentdb/cli/agentdb-cli.js'
# Then use: agentdb-new --help
```

## Current Directory Issue

You tried to run from: `/workspaces/agent-control-plane/packages/agentdb`
But the CLI is in: `/workspaces/agent-control-plane/agent-control-plane`

The `packages/agentdb` directory is the OLD AgentDB package.
The NEW CLI with frontier features is in the `agent-control-plane` subdirectory.

## Quick Test

```bash
# From anywhere:
cd /workspaces/agent-control-plane/agent-control-plane
node dist/agentdb/cli/agentdb-cli.js --help

# Should show:
# █▀█ █▀▀ █▀▀ █▄░█ ▀█▀ █▀▄ █▄▄
# █▀█ █▄█ ██▄ █░▀█ ░█░ █▄▀ █▄█
#
# AgentDB CLI - Frontier Memory Features
```

## Run the Test Suite

```bash
cd /workspaces/agent-control-plane/agent-control-plane
./scripts/test-agentdb.sh
```

## Directory Structure

```
/workspaces/agent-control-plane/
├── agent-control-plane/              ← NEW CLI IS HERE
│   ├── dist/
│   │   └── agentdb/
│   │       └── cli/
│   │           └── agentdb-cli.js  ← THIS IS THE NEW CLI
│   ├── scripts/
│   │   └── test-agentdb.sh
│   └── run-agentdb.sh         ← USE THIS LAUNCHER
│
└── packages/
    └── agentdb/               ← OLD PACKAGE (ignore)
```
